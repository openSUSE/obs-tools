version: "2.0"
services:
  openqa-trigger:
    build:
      dockerfile: Dockerfile
      context: openqa-trigger
    restart: always
    environment:
      - RUN_EVERY=600
    volumes:
      - .:/home/frontend/openqa-trigger
